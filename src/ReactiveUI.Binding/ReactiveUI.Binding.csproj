<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFrameworks>$(BindingRuntimeTargets)</TargetFrameworks>
    <PackageDescription>Runtime library for ReactiveUI property observation and binding. Provides ICreatesObservableForProperty, expression chain decomposition, and converter infrastructure.</PackageDescription>
    <PackageTags>reactiveui;binding;observableforproperty</PackageTags>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="ReactiveUI.Binding.Tests"/>
    <InternalsVisibleTo Include="ReactiveUI.Binding.GeneratedCode.Tests"/>
    <InternalsVisibleTo Include="ReactiveUI.Binding.Wpf"/>
    <InternalsVisibleTo Include="ReactiveUI.Binding.WinForms"/>
    <InternalsVisibleTo Include="ReactiveUI.Binding.Maui"/>
    <InternalsVisibleTo Include="ReactiveUI.Binding.Reactive"/>
  </ItemGroup>

  <ItemGroup>
    <Using Include="System.Diagnostics"/>
    <Using Include="System.Diagnostics.CodeAnalysis"/>
    <Using Include="System.Linq.Expressions"/>
    <Using Include="System.Runtime.CompilerServices"/>
    <Using Include="Splat"/>
    <Using Include="ReactiveUI.Binding.Helpers"/>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Splat"/>
    <PackageReference Include="ReactiveUI.Primitives"/>
  </ItemGroup>

  <!-- DateOnly and TimeOnly only exist on .NET 6+. Their converters are guarded with
       #if NET6_0_OR_GREATER, so exclude the sources outright on the older targets rather than
       compiling files that declare nothing there. -->
  <ItemGroup Condition="!$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net6.0'))">
    <Compile Remove="Bindings\Converter\*DateOnly*.cs"/>
    <Compile Remove="Bindings\Converter\*TimeOnly*.cs"/>
  </ItemGroup>

  <!-- NotNullAttribute ships in the framework from netcoreapp3.0/netstandard2.1 onwards; the
       polyfill is only needed below that. -->
  <ItemGroup Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'netcoreapp3.0'))">
    <Compile Remove="Polyfills\NotNullAttribute.cs"/>
  </ItemGroup>

  <!-- Index and Range back the ^ and .. operators on the targets that predate them; .NET 5+ ships
       both, where these sources are guarded out entirely. -->
  <ItemGroup Condition="$([MSBuild]::IsTargetFrameworkCompatible('$(TargetFramework)', 'net5.0'))">
    <Compile Remove="Polyfills\Index.cs"/>
    <Compile Remove="Polyfills\Range.cs"/>
  </ItemGroup>
</Project>
